#include <iostream>
using namespace std;


    
int main() {

    double nilM, nilB;
    cout << "masukan nilai matematika : ";
    cin >> nilM;
    cout << "masukan nilai bahasa inggris : ";
    cin >> nilB;
    // rerata = (nilaiM + nilaiB) /2;
    // rata = rerata (nilaiM, nilaiB);
    cout << "status Asroni diterima, Lia diterima, joko ditolak"

        int main() {
                  matematika_nilai = pendaftar "matematika"
                      inggring_score = pendafttar "inggris"

                      if avg_score >= 70 or math_score > 80:
                  pendaftar[status] = "diterima"
                      diterima += 1
                      else:
                  pendaftar[status] = "ditolak"
                      ditolak += 1
                      // display total kadinat dan kadinat yang tidak di terima 
              }